version: '3'

services:
  blue:
    image: "${IMAGE_TAG}"
    container_name: blue
    ports:
      - "3001:3000"
    environment:
      - ENVIRONMENT=blue

  green:
    image: "${IMAGE_TAG}"
    container_name: green
    ports:
      - "3002:3000"
    environment:
      - ENVIRONMENT=green
